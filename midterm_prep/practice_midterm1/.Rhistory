knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(janitor)
elephants <- read_csv("data/ElephantsMF")
getwd()
elephants <- read_csv("data/ElephantsMF,csv")
elephants <- read_csv("data/ElephantsMF.csv")
names(elephants)
class(elephants$Age)
class(elephants$Height)
str(elephants)
dim(elephants)
table(elephants)
glimpse(elephants)
elephants <- select_all(elephants, tolower())
elephants <- select_all(elephants, tolower
elephants <- select_all(elephants, tolower)
elephants <- as.factor(elephants$sex)
class(elephants$sex)
class(sex)
names(elephants)
names(elephants)
elephants <- read_csv("data/ElephantsMF.csv")
names(elephants)
names(elephants) <- tolower(names, elephants)
names(elephants) <- tolower(names(elephants))
elephants <- as.factor(elephants$sex)
names(elephants)
view(elephants)
elephants <- clean_names(elephants)
elephants <- read_csv("data/ElephantsMF.csv")
elephants <- clean_names(elephants)
elephants$sex <- as.factor(elephants$sex)
names(elephants)
elephants %>%
count(sex)
elephants %>%
group_by(age) %>%
summarise(mean_age = mean(age))
elephants %>%
summarise(mean_age = mean(age))
elephants %>%
group_by(sex) %>%
summarise(mean_age = mean(age),
mean_height = mean(height))
elephants %>%
filter(age >20) %>%
group_by(sex) %>%
summarise(mean_height = mean(height),
min_height = min(height),
max_height = max(height))
elephants %>%
filter(age >20) %>%
group_by(sex) %>%
summarise(mean_height = mean(height),
min_height = min(height),
max_height = max(height),
total = n())
read_csv("data/IvindoData_DryadVersion.csv")
dry_adversion <- read_csv("data/IvindoData_DryadVersion.csv")
dim(dry_adversion)
glimpse(dry_adversion)
names(dry_adversion)
dry_adversion <- clean_names(dry_adversion)
dry_adversion$hunt_cat <- as.factor(dry_adversion$hunt_cat)
dry_adversion$land_use <- as.factor(dry_adversion$land_use)
names(dry_adversion)
View(dry_adversion)
dry_adversion %>%
filter(hunt_cat == "High") %>%
summarise(mean_bird = mean(diversity_bird_species),
mean_mammal = mean(diversity_mammal_species),
samples = n())
dry_adversion %>%
filter(hunt_cat == "Moderate") %>%
summarise(mean_bird = mean(diversity_bird_species),
mean_mammal = mean(diversity_mammal_species),
samples = n())
dry_adversion %>%
filter(distance<3) %>%
summarise(across(contains("ra"), mean)
dry_adversion %>%
filter(distance<3) %>%
summarise(across(contains("ra"), mean))
dry_adversion %>%
filter(distance>25) %>%
summarise(across(contains("ra"), mean))
